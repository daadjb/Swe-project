name: Node.js CI/CD Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js (Version 20)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      # 🛡️ الاختبارات غير الوظيفية - فحص الأمان (Non-functional Test)
      - name: Run Security Audit (allowing moderate)
        run: npm audit --audit-level=high 

      # 🧪 الاختبارات الوظيفية وتغطية الكود (Functional Tests)
      - name: Run Vitest and generate report
        run: npm run test:coverage

      # 📄 رفع تقرير التغطية كـ Artifact
      - name: Upload coverage report Artifact
        uses: actions/upload-artifact@v4
        with:
          name: vitest-coverage-report
          path: coverage/
